applicationType: default
defaultPackage: com.zacharykarpinski
testFramework: junit
sourceLanguage: kotlin
buildTool: gradle
features: [annotation-api, app-name, data, h2, gradle, http-client, junit, kotlin, kotlin-application, logback, netty-server, readme, shade, yaml]
micronaut:
  server:
    netty:
      logLevel: debug