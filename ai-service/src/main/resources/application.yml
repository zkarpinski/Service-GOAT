micronaut:
  application:
    name: aiService
  server:
    port: 8082
netty:
  default:
    allocator:
      max-order: 3
consul:
  client:
    registration:
      enabled: true
    defaultZone: "${CONSUL_HOST:localhost}:${CONSUL_PORT:8500}"
