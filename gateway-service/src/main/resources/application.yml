micronaut:
  application:
    name: gatewayService
  server:
    port: 8080
netty:
  default:
    allocator:
      max-order: 3
consul:
  client:
    registration:
      enabled: true
    defaultZone: "${CONSUL_HOST:localhost}:${CONSUL_PORT:8500}"